document.write("<script src='js/aes.js' type='text/javascript'></script>");
document.write("<script src='js/mode-ecb.js' type='text/javascript'></script>");
function Encrypt(obj, noStringify) {
    var word = noStringify ? obj : JSON.stringify(obj);

    var key = CryptoJS.enc.Utf8.parse("abcdefgabcdefg12");

    var srcs = CryptoJS.enc.Utf8.parse(word);
    var encrypted = CryptoJS.AES.encrypt(srcs, key, {mode: CryptoJS.mode.ECB, padding: CryptoJS.pad.Pkcs7});
    return encrypted.toString();
}
function Decrypt(word) {
    if (word === undefined)return undefined;

    var key = CryptoJS.enc.Utf8.parse("abcdefgabcdefg12");

    var decrypt = CryptoJS.AES.decrypt(word, key, {mode: CryptoJS.mode.ECB, padding: CryptoJS.pad.Pkcs7});
    return /*JSON.parse*/(CryptoJS.enc.Utf8.stringify(decrypt).toString());
}